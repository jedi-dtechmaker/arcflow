module.exports = {
    // An empty plugins object prevents Vite from walking up the directory tree
    // and picking up an old PostCSS/Tailwind v3 config from a parent folder,
    // which was conflicting with Tailwind v4 used in this project.
    plugins: {},
};
